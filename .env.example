# MongoDB Atlas Configuration
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/datazen?retryWrites=true&w=majority

# Feature #1 API (optional - system uses fallback if unavailable)
FEATURE1_BASE_URL=http://localhost:8001

# Mock mode (true = no database needed, use fake data)
USE_MOCK_DATA=false

# Server
PORT=8000
